# Agent notes

## UI changes

When you change the UI, show the result with screenshots before wrapping up. Capture the updated view in the browser (light and dark if the change is theme-sensitive) and include the images in the reply and the PR.
